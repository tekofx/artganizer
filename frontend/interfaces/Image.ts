export default interface Image {
    date: Date,
    description: string,
    format: string,
    height: number,
    id: number,
    image: string,
    rating: number,
    title: string,
    width: number,
}
