export interface Settings {
  galleryInfo: GalleryInfo;
}

export interface GalleryInfo {
  title: boolean;
  tags: boolean;
  rating: boolean;
  artist: boolean;
  dimensions: boolean;
  date: boolean;
  colors: boolean;
}
